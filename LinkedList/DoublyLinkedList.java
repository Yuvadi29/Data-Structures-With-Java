/*Doubly LinkedList are the linkedLists which contains a prev node and a next node as well. With this we are able to know the previous node of the current node. */

public class DoublyLinkedList {
    
    class DoublyLinkedList {
        int val;
        DoublyLinkedList next, prev;
        
        DoublyLinkedList(int x){
            this.val = x;
        }
    }
}
